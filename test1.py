import numpy as np
import matplotlib.pyplot as plt

mean = 0
variance = 1

std_dev = np.random.normal(0, np.sqrt(1))
print(std_dev)

result= np.random.normal(0,1)
print(result)

# print(np.mean(result))
# print(np.var(result))

# x = [x for x in range(10)]
# y = result

# print(x , y)
# plt.plot(x, y, '-r')
# plt.show()